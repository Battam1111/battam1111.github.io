<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://battam1111.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://battam1111.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-05-23T12:06:23+00:00</updated><id>https://battam1111.github.io/feed.xml</id><title type="html">blank</title><subtitle>Yanjun Chen (陈彦筠), second-year PhD student at The Hong Kong Polytechnic University. I work on Environment-Centric AI: the design of environments in which intelligent agents learn, across LLMs, reinforcement learning, and embodied systems. </subtitle><entry><title type="html">Why Environment-Centric AI</title><link href="https://battam1111.github.io/blog/2026/why-environment-centric-ai/" rel="alternate" type="text/html" title="Why Environment-Centric AI"/><published>2026-05-23T00:00:00+00:00</published><updated>2026-05-23T00:00:00+00:00</updated><id>https://battam1111.github.io/blog/2026/why-environment-centric-ai</id><content type="html" xml:base="https://battam1111.github.io/blog/2026/why-environment-centric-ai/"><![CDATA[<p>Here is a question I keep returning to.</p> <p>When a language model trained with RLHF refuses to ground its answer in the source material it was supposed to read, what was <em>actually</em> wrong?</p> <p>The reflex answer is that the model is too small, the algorithm is too crude, or the reward signal was noisy. All of these can be true. But none of them, on their own, explain why the same base model trained under a slightly different setup <em>would have</em> learned to ground its answer in the source. The thing that changed across those two runs is not the model and not the algorithm. It is the <strong>environment</strong> the model was trained in: what evidence it saw, what objective it was rewarded against, at what granularity that reward arrived, on which task distribution, evaluated against which criterion.</p> <p>Most LLM research talks about these as hyperparameters or implementation details. I think they are the object of study.</p> <h2 id="the-sutton-boundary-applied">The Sutton boundary, applied</h2> <p>Sutton and Barto’s definition of an RL environment is strict: the environment is everything outside the agent that the agent cannot unilaterally alter. Rewards are external because they define the task; the agent cannot redefine its own task. The boundary is an <em>analytical</em> choice, but the rule is consistent.</p> <p>Apply this rule to LLM post-training:</p> <ul> <li>The model’s weights are the agent.</li> <li>The optimization algorithm (PPO, GRPO, DPO) is the procedure by which the agent updates itself.</li> <li>Everything else, <strong>the reward model, the task distribution, the observation format, the curriculum, the verifier, the evaluation set</strong>, is the environment.</li> </ul> <p>That last list is what most papers casually call “the training setup”. I want to call it what it is.</p> <h2 id="a-specific-structural-property">A specific structural property</h2> <p>LLM environments have a property classical RL environments do not. The transition function is degenerate. Given the current token sequence and the next token sampled, the next state is exactly <code class="language-plaintext highlighter-rouge">concat(state, token)</code>. There is no stochastic dynamics on the environment side; all the randomness is in the agent’s sampling.</p> <p>This is the strange and useful part. In classical RL, environments are interesting because their dynamics are interesting. In LLMs, the dynamics are trivial. What is left for the environment to do? Three things, all on the interface:</p> <ol> <li>Decide what evidence the model receives (observation interface).</li> <li>Decide what behavior is rewarded (reward interface).</li> <li>Decide which problems the model sees and is evaluated on (distribution and evaluation interface).</li> </ol> <p>That is exactly where the variation across LLM training setups lives. It is not a coincidence that the levers researchers actually pull, prompt formatting, RM choice, RLVR versus RLHF, outcome versus process reward, curriculum, eval coverage, all land on those three interfaces. The degenerate dynamics force the design surface there.</p> <h2 id="what-this-changes">What this changes</h2> <p>If the environment is the object of study, then the first question for any failure mode is not “what should we change about the model” but <strong>“what about the environment made this failure rational”</strong>. The reward model trained on length-biased preferences is the environment teaching the model that longer is better. The benchmark that scores accuracy without checking grounding is the environment telling the model that fluent guesses count as correct.</p> <p>The grounding failure is not a model failure. It is the model’s correct response to a specific environment.</p> <p>This reframe gives you two things at once: a diagnostic posture (decompose the environment, locate the lever) and a redesign posture (change the lever, predict the effect). Most current work has the diagnostic part implicit. I want to make it explicit and operational.</p> <h2 id="the-longer-bet">The longer bet</h2> <p>The bigger version of this is harder to defend in a single note, so I will only state it: I think the next jump in what AI systems can do will come from making the <em>environment</em> itself the thing that scales, trains, and generalizes, the way models have. Today the model is a first-class object that scales gracefully and that we know how to train. The environment is a hand-crafted artifact, fragile and bespoke. That asymmetry is where the headroom is.</p> <p>My two papers so far are early stakes in that direction. The Accuracy Paradox in RLHF (EMNLP 2024) shows that a single environment-side property, reward-model accuracy, has non-monotonic effects on the agent it trains. C3, Exact Is Easier (in submission), exploits the degenerate-transition structure to make per-agent credit exactly computable in cooperative multi-agent LLM environments, replacing every approximate critic and trajectory baseline. Both are about treating the environment as a designed object and reaching for what falls out when you do.</p> <p>What I am building toward in the next year is a framework for decomposing LLM training environments along a small set of axes, showing that the axis values predict which failure modes occur, and then closing the loop by re-designing the environment to fix the failure. If that sounds like a long arc, it is. This is a note about <em>why</em>, not a roadmap.</p> <p>I will write more on the <em>how</em> when there is more of it to write.</p> <hr/> <p><em>This is the first note. If you want the formal pieces, they are on the <a href="/publications/">publications page</a>. If you want to argue, I am at <a href="mailto:yan-jun.chen@connect.polyu.hk">yan-jun.chen@connect.polyu.hk</a>.</em></p>]]></content><author><name></name></author><category term="research"/><category term="research"/><category term="environment-centric-ai"/><category term="framing"/><summary type="html"><![CDATA[A short statement of why I treat the training environment as a first-class object of study, and what that buys you over the usual model-centric reading of LLM training.]]></summary></entry></feed>